import { StyleSheet, Text, View } from "react-native";
import React from "react";

const BalanceDashboard = () => {
  return (
    <View style={{ height: "45%" }}>
      <Text>BalanceDashboard</Text>
    </View>
  );
};

export default BalanceDashboard;

const styles = StyleSheet.create({});
